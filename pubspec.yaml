name: flutter_todo_app
description: Todo app flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.3
  freezed_annotation: ^0.14.2
  shared_preferences: ^2.0.6
  state_notifier: ^0.7.0
  flutter_hooks: ^0.18.0
  hooks_riverpod: ^1.0.0-dev.6

dev_dependencies:
  build_runner: ^2.1.0
  flutter_test:
    sdk: flutter
  freezed: ^0.14.2
  json_serializable: ^4.1.4

flutter:
  uses-material-design: true
